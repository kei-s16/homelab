all:
  hosts:
  vars:
    ansible_user: maintainer

  children:
    site02:
      children:
        archlinux:
          children:
            arch:
              hosts:
                192.168.32.100:
            archarm:
              hosts:
                192.168.32.30:
                192.168.32.31:

        lxc:
          hosts:
            192.168.32.100:

        kubernetes:
          children:
            kubernetes_proxy:
            kubernetes_worker:
            kubernetes_master:

        bastion:
          hosts:

        docker:
          hosts:

        grafana:
          hosts:
            192.168.32.30:

        dns:
          hosts:
            192.168.32.100:

